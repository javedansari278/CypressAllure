{
  "name": "cypress-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prerun": "rimraf allure-results && rimraf allure-report",
    "run": "npx cypress run",
    "postrun": "allure generate && allure open"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^10.3.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.28.5",
    "allure-commandline": "^2.18.1",
    "mocha": "^10.0.0"
  }
}
